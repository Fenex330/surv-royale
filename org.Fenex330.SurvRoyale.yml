app-id: org.Fenex330.SurvRoyale
runtime: org.freedesktop.Platform
runtime-version: '21.08'
sdk: org.freedesktop.Sdk
command: AppRun
modules:
  - name: SurvRoyale
    buildsystem: make
    build-commands:
      - mkdir -p app
        cp -r build/AppDir/* app
    sources:
      - type: directory
        path: .
