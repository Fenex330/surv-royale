#include "headers.hpp"

Player::Player() : active(true),
                   slot(1),
                   crosshair_distance(0.0),
                   rotation(0.0),
                   x(0),
                   y(0)
{
    //
}

Player::interact()
{
    //
}

Player::fire()
{
    //
}
